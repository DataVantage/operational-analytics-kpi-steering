# assets

Screenshots for the main README.

After publishing the dashboard, take a full-page screenshot of
`dashboard/index.html`, save it here as `dashboard.png`, and uncomment the image
line near the top of the root `README.md`.

A recruiter who does not clone the repository will see that screenshot and
nothing else. It is worth ten minutes.
